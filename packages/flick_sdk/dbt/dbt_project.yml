name: flick_dbt
version: "1.0.0"
profile: flick

model-paths: ["models"]
test-paths:  ["tests"]
seed-paths:  ["seeds"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  flick_dbt:
    staging:
      +materialized: incremental
      +schema: silver
    marts:
      gold:
        +materialized: table
        +schema: gold
